name: websend-dev
services:
  shell:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: ["/app/entrypoint.sh"]
    stdin_open: true
    tty: true
    network_mode: "host"
    environment:
      SERVER_PORT: 3000
    volumes:
      - .:/app
