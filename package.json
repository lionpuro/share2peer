{
	"name": "share2peer",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"workspaces": [
		"backend",
		"frontend"
	],
	"scripts": {
		"build": "npm run build:frontend",
		"build:frontend": "npm run build -w frontend",
		"lint": "npm run lint --workspaces --if-present",
		"format": "prettier --write . && gofmt -l -s -w backend"
	},
	"devDependencies": {
		"prettier": "^3.6.2",
		"prettier-plugin-tailwindcss": "^0.7.1"
	}
}
