{
	"name": "share2peer",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"workspaces": [
		"web"
	],
	"scripts": {
		"build": "npm run build:web",
		"build:web": "npm run build -w web",
		"lint": "npm run lint --workspaces --if-present",
		"format": "prettier --write . && gofmt -l -s -w server"
	},
	"devDependencies": {
		"prettier": "^3.6.2",
		"prettier-plugin-tailwindcss": "^0.7.1"
	}
}
