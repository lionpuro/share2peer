{
	"name": "share2peer",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"workspaces": [
		"web"
	],
	"scripts": {
		"format": "prettier --write . && gofmt -l -s -w server"
	},
	"devDependencies": {
		"prettier": "^3.6.2",
		"prettier-plugin-tailwindcss": "^0.7.1"
	}
}
